function longestPalindrome(s: string): string {
    let res: string = '';
    let cur_res: string = '';

    const max = s.length;

    for (let i: number = 0; i < max; i++) {

    }

    return cur_res;
};

export default {};